node_modules
.next
.env.local
.vercel

# Build cache TypeScript
tsconfig.tsbuildinfo

# Préférences locales Claude Code
.claude/settings.local.json

# Sauvegardes de tables avant suppression (données métier — ne pas commiter)
/backup_scoa_vente_2026-07-16/

# Documents source comptables (ne pas commiter — données sensibles)
/Releve paiement/
/custumer return/
/fichier removal order/
/settlement/
/Amazon/
/vue/

# Projets source deposes pour integration (ne pas commiter)
/design/

# Rapports de vente Traction telecharges chaque mois (donnees metier)
/analyse vente 2891/

# Cache local du CLI Supabase
/supabase/.temp/

# Scripts jetables de session
.tmp-*
